Construye la utilidad ffmpeg para Ubuntu 14.04 mediante un script definido dentro del fichero Vagrantfile.
Basta con ejectutar "vagrant up". El resultado es el fichero ffmpeg.tar.gz en la máquina hosts.
Este fichero luego lo utilizamos para un buildpacks de Heroku.